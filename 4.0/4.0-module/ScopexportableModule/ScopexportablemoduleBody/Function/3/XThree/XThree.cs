using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleBody
    {
        public void XThree()
        {
            var value = XTriple.ForgeLevel(ScopexportablemoduleBodycycle.XSecondary.XDouble.LevelArray, (Scopexportableformheadersolid[])ScopexportablemoduleBodycycle.XZeroth.ObjectArray[2]);

            XThird xthird;

            xthird = new XThird(value);

            ScopexportablemoduleBodycycle.XTertiary = xthird;

            return;
        }
    }
}
