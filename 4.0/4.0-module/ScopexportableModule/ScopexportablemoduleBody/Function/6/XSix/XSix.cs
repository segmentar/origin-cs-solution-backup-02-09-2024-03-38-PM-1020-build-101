using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleBody
    {
        public void XSix()
        {
            var value = XSextuple.ForgeLevel(ScopexportablemoduleBodycycle.XQuinary.XQuintuple.LevelArray, (Scopexportableformheadersolid[])ScopexportablemoduleBodycycle.XZeroth.ObjectArray[2]);

            XSixth xsixth;

            xsixth = new XSixth(value);

            ScopexportablemoduleBodycycle.XSenary = xsixth;

            return;
        }
    }
}
