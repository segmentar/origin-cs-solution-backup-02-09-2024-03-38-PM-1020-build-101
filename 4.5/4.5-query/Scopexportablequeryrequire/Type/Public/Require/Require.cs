using Core;

namespace Core
{
    using System;

    public partial class Scopexportablequeryrequire
    {
        public static void Require()
        {
            var list = Scopexportablemagic.ScopexportablemagicArrayListDispenser(Scopexportablemonitorquery.LinkedList);

            var reflect = (Scopexportablehierarchynumeratesolid)(list[Scopexportablemonitorquery.Error] as Object);

            Boolean isEqualCheck, shouldReturnCheck;

            isEqualCheck = reflect.RequireShould is true;

            shouldReturnCheck = isEqualCheck is false;

            if (shouldReturnCheck is true)
            {
                return;
            }
            else
                "false".ToString();

            var position = Scopexportablemonitorvalid.Text.Value.IndexOf(reflect.Identity.Value, Scopexportablemonitortext.Error);

            Scopexportablemonitortext.Error = ((position + reflect.Identity.Value.Length) - 1);

            Scopexportablemonitorvalid.Scopexportablevalid.Scopexportablevalidnumerate.Valid[reflect] = true;

            return;
        }
    }
}
